# Future Engine — Progress Log

## Instrução para agentes
Leia este arquivo no início de cada sessão. Atualize ao final, antes do commit.
Nunca sobrescreva entradas anteriores — apenas acrescente ao final.

---

## Estado atual do projeto

**Última sessão**: [preencher]
**Features passing**: 0 / 20
**Branch atual**: main
**Último commit**: [preencher após primeiro commit]

---

## Sessão 000 — Setup inicial (harness)

**Data**: 2026-03-17
**Agente**: Initializer
**Objetivo**: Criar infraestrutura do harness

**O que foi feito**:
- Criado feature_list.json com 20 features mapeadas
- Criado este arquivo de progresso
- Criado init.sh para startup reproduzível
- Criado prompts de initializer e coding agent
- Identificados 5 bugs críticos no código existente:
  1. STORE in-memory (F002 falha após restart)
  2. avg_price sobrescreve ao invés de VWAP (F014)
  3. signal_engine usa math.sin — sem dados reais (F004-F006)
  4. unrealized_pnl usa 1% fixo (F016)
  5. Sem validação de quantity <= 0 antes de registrar ordem (F013)

**Estado ao final**:
- API funcional em modo paper
- Todos os 20 features marcados como passes: false
- Ambiente Docker funcional via docker-compose

**Para a próxima sessão**:
- Prioridade 1: Migrar STORE para SQLite (F002)
- Prioridade 2: Corrigir VWAP em portfolio_engine (F014)
- Prioridade 3: Adicionar guard de quantity=0 em execution_engine (F013)
- Depois: Substituir signal_engine mock por dados reais (F004-F006)

---

## [Template para próximas sessões]

## Sessão NNN — [título]

**Data**: 
**Agente**: Coding Agent
**Objetivo**: 

**Features trabalhadas**:
- [ ] F0XX: [descrição]

**O que foi feito**:
- 

**Bugs encontrados**:
- 

**Estado ao final**:
- Features passing: X / 20
- Testes rodados: 

**Para a próxima sessão**:
- 

---

## Sessão 001 — Infraestrutura da API (F001)

**Data**: 2026-03-18
**Agente**: Coding Agent
**Objetivo**: Implementar F001 — API inicia sem erros e retorna health check válido

**Features trabalhadas**:
- [x] F001: passes=true

**O que foi feito**:
- Criado app/main.py com FastAPI e todos os routers registrados
- Criado app/core/config.py com Settings (pydantic-settings) para EXCHANGE_MODE, LIVE_TRADING_ENABLED, etc.
- Criado app/services/signal_engine.py — geração determinística de sinais por símbolo
- Criado app/services/risk_engine.py — filtro de confidence < 0.62 e action='hold'
- Criado app/services/backtest_engine.py — métricas de backtest com convergência por períodos
- Criados todos os routers: health, signals, autotrade, ops, portfolio, backtest, scheduler
- Criados Dockerfile, docker-compose.yml, requirements.txt, .env.example
- Criados __init__.py para app/, app/core/, app/services/, app/routers/

**Bugs encontrados**:
- Docker Hub inacessível no ambiente de desenvolvimento (sem acesso à internet para pull de imagens) — verificação F001 feita com uvicorn direto

**Estado ao final**:
- Features passing: 1 / 20 (F001)
- API responde em /health com {"status": "ok", "env": {...}}
- Smoke test POST /signals/generate passou
- Sinais gerados com prob_up + prob_down == 1.0, ordenados por confidence

**Para a próxima sessão**:
- Prioridade: F002 — persistência SQLite entre restarts (store.py já implementado, testar com restart)
- F011: verificar status='executed' em paper mode (já funcional — testar end-to-end)
- F013: verificar rejeição de quantity=0 (já implementado em execution_engine.py)
