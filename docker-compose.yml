services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_PATH=/data/future_engine.db
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data:
